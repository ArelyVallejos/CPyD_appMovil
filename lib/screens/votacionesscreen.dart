import 'package:flutter/material.dart';

class VotacionesScreen extends StatelessWidget {
  const VotacionesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Votaciones");
  }
}
